services:
  - type: web
    runtime: node
    name: fertintelligence-image-api
    branch: m-fertilization
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ALLOWED_ORIGINS
        value: http://localhost:5173,https://fertintelligence-client.onrender.com
      - key: MONGODB_URI
        sync: false
